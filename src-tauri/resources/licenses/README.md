# Bundled tool licenses

The Windows tool preparation script copies the exact upstream license files for the generated FFmpeg and whisper.cpp binaries into the `generated` directory. Tauri includes this directory in release packages.
